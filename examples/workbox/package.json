{
  "name": "workbox",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "start-server-and-test 'docker run -p 3000:80 -e FOO=workbox runtime-env-example' 3000 'cypress run' && docker ps -f ancestor=runtime-env-example -q | xargs docker rm -f"
  },
  "devDependencies": {
    "cypress": "15.8.1",
    "html-webpack-plugin": "5.6.5",
    "start-server-and-test": "2.1.3",
    "webpack": "5.104.1",
    "webpack-cli": "6.0.1",
    "workbox-webpack-plugin": "7.4.0"
  }
}
