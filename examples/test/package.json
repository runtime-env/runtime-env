{
  "name": "example",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "dotenv -e .env -- runtime-env gen-js --output-file jest-setup-file-runtime-env.js && jest"
  },
  "devDependencies": {
    "@runtime-env/cli": "^0.6.0",
    "dotenv-cli": "^7.4.1",
    "jest": "^29.7.0"
  }
}
