{
  "name": "example",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "runtime-env gen-js --mode test && jest"
  },
  "devDependencies": {
    "@runtime-env/cli": "^0.1.4",
    "jest": "^29.7.0"
  }
}
