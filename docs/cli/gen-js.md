# `gen-js`

Generate JavaScript assigning runtime values to `globalThis.<name>`.

## Command

```bash
npx @runtime-env/cli gen-js [--env-file <envFile...>] [--output-file <path>]
```

Reads schema, validates env, and outputs script content.
