# `interpolate`

Interpolate template placeholders with runtime values.

## Command

```bash
npx @runtime-env/cli interpolate [--env-file <envFile...>] [--input-file <path>] [--output-file <path>]
```

If `--input-file` is not provided, the first positional argument is used as input.
