## 1. Implementation

- [x] 1.1 Create `packages/vite-plugin/README.md` following the React.dev style:
  - Add a "Setup" section covering prerequisites:
    - Creating `.runtimeenvschema.json`.
    - Creating `.env`.
    - Adding `<script src="/runtime-env.js"></script>` to `index.html`.
  - Explain the zero-config behavior (auto `gen-js`, `interpolateIndexHtml`, `.env` loading, `gen-ts`).
  - Link to the root README for CLI documentation.

## 2. Verification

- [x] 2.1 Review the content to ensure it meets the React documentation standard:
  - High signal-to-noise ratio.
  - Practical, copy-pasteable examples.
  - Clear explanation of the "Build once, deploy anywhere" philosophy.
