## 1. Implementation

- [x] 1.1 Update `packages/vite-plugin/package.json` peerDependencies for `vite` and `vitest` to `*`.

## 2. Verification

- [x] 2.1 Run `npm install` in the root to ensure no conflicts.
- [x] 2.2 Verify that `comprehensive-vite` example still works (build/test).
