## 1. Implementation

- [x] 1.1 Dev: run `transformIndexHtml` with `order: "pre"`.
- [x] 1.2 Build: replace placeholders with tokens in a `pre` `transformIndexHtml` and restore them in emitted HTML assets.

## 2. Verification

- [x] 2.1 Add a CI step for `tests/issues/html-placeholder-in-body/`.
- [x] 2.2 Run `tests/issues/html-placeholder-in-body/` and the existing e2e examples.
