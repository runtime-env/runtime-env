## 1. Implementation

- [x] 1.1 Preview: run `gen-js` and interpolate `index.html` in `configurePreviewServer`, and serve the cached results from the middleware.
- [x] 1.2 Document in `docs/vite/preview.md` that `.env` changes require restarting the preview server.

## 2. Verification

- [x] 2.1 Run the existing e2e examples and `tests/`.
